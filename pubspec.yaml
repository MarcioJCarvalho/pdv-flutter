name: pdv_flutter
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite:
  get_it: 5.0.6
  path:
  # Plugin de gestão de estado
  mobx: 1.2.1+4
  # Para que a gente consiga usar o Widget Observer (Para observar o estado)
  flutter_mobx: 1.1.0+2
  mask_text_input_formatter: ^1.2.1
  cloud_firestore: ^0.16.0+1
  firebase_core: ^0.7.0

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Para conseguirmos ultiliar o @observable e @action
  mobx_codegen: 1.1.2
  # Ajuda na criação do arquivo .g
  build_runner: 1.11.1

flutter:
  uses-material-design: true

